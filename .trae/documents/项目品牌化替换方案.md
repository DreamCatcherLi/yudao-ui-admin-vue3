我已分析了项目结构，找到了定义“芋道管理系统”和相关标识的核心位置。为了将“芋道管理系统”替换为“艾伦集团”，并更新相关标识，我制定了以下修改方案：

### 核心修改计划

1. **全局环境配置 (.env)**

   * 修改 `VITE_APP_TITLE`：将 `芋道管理系统` 修改为 `艾伦集团`。

   * 修改 `VITE_APP_DEFAULT_LOGIN_TENANT`：将 `芋道源码` 修改为 `艾伦集团`（这将改变默认登录的租户名）。

2. **首页元数据 (index.html)**

   * 更新 `<meta name="keywords">`：替换其中的 `芋道管理系统`。

   * 更新 `<meta name="description">`：替换其中的 `芋道管理系统`。

   * （注：`<title>` 标签已动态绑定环境变量，无需手动修改）。

3. **登录页面组件 (Hardcoded Text)**

   * **MobileForm.vue** (`src/views/Login/components/MobileForm.vue`)：修改默认 `tenantName` 为 `艾伦集团`。

   * **SocialLogin.vue** (`src/views/Login/SocialLogin.vue`)：修改默认 `tenantName` 为 `艾伦集团`。

此方案将覆盖系统标题、浏览器标签页、SEO 信息以及登录页面的默认显示。请确认是否执行此方案？
